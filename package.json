{
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "turbo": "^1.13.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main.js"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/mapped-types": "*",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
